import { MaskMoneyDirective } from './mask-money.directive';

describe('MaskMoneyDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskMoneyDirective();
    expect(directive).toBeTruthy();
  });
});
