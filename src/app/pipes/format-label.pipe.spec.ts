import { FormatLabelPipe } from './format-label.pipe';

describe('FormatLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
